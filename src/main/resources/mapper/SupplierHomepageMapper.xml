<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    WHAT IS THIS FILE?
    ══════════════════
    XML Mapper for SupplierHomepageRepository.
    All queries return simple types (Integer) — no resultMap needed.

    These queries power the supplier dashboard homepage:
    - Order counts by status
    - Product import/sold totals by date range
-->

<mapper namespace="com.henheang.hphsar.repository.SupplierHomepageRepository">

    <!-- ─── STORE ──────────────────────────────────────────────────────────── -->

    <select id="getStoreId" resultType="int">
        SELECT id FROM tb_store WHERE supplier_account_id = #{currentUserId}
    </select>

    <!-- ─── ORDER COUNTS BY STATUS (single query) ────────────────────────── -->
    <!--
        Returns all 5 status counts in one DB round-trip using CASE WHEN.
        Step 3C: status_id 1=New, 2=Preparing, 3=Dispatching, 4=Confirming (id
        retired — no code path ever writes it, always 0 by design), 6=Completed
        (was checked against dead id 5 — this "completed" tile was always 0).
    -->
    <resultMap id="SupplierHomepageResultMap" type="com.henheang.hphsar.model.supplier.SupplierHomepage">
        <result property="newOrder"   column="new_order"/>
        <result property="preparing"  column="preparing"/>
        <result property="dispatch"   column="dispatch"/>
        <result property="confirming" column="confirming"/>
        <result property="completed"  column="completed"/>
    </resultMap>

    <select id="getHomepageCounts" resultMap="SupplierHomepageResultMap">
        SELECT
            COUNT(CASE WHEN status_id = 1 THEN 1 END) AS new_order,
            COUNT(CASE WHEN status_id = 2 THEN 1 END) AS preparing,
            COUNT(CASE WHEN status_id = 3 THEN 1 END) AS dispatch,
            COUNT(CASE WHEN status_id = 4 THEN 1 END) AS confirming,
            COUNT(CASE WHEN status_id = 6 THEN 1 END) AS completed
        FROM tb_order
        WHERE store_id = #{storeId}
    </select>

    <select id="getNewOrder" resultType="int">
        SELECT count(*) AS newOrder FROM tb_order WHERE store_id = #{storeId} AND status_id = 1
    </select>

    <select id="getPreparing" resultType="int">
        SELECT count(*) AS preparing FROM tb_order WHERE store_id = #{storeId} AND status_id = 2
    </select>

    <select id="getDispatch" resultType="int">
        SELECT count(*) AS dispatch FROM tb_order WHERE store_id = #{storeId} AND status_id = 3
    </select>

    <!-- Retired (Step 3C) — no separate "confirming" substate in the redesigned
         lifecycle. Always 0 by design; see SupplierOrderMapper#getConfirmingOrders. -->
    <select id="getConfirming" resultType="int">
        SELECT count(*) AS confirming FROM tb_order WHERE store_id = #{storeId} AND status_id = 4
    </select>

    <!-- Fixed (Step 3C): was status_id = 5, a dead id nothing ever wrote — this
         tile was always 0. COMPLETED is id 6. -->
    <select id="getCompleted" resultType="int">
        SELECT count(*) AS completed FROM tb_order WHERE store_id = #{storeId} AND status_id = 6
    </select>

    <!-- ─── MONTHLY STATS ──────────────────────────────────────────────────── -->
    <!--
        ${startDate}-01 and ${endDate}-01 → string substitution to build date range
        date_trunc('month', ...) → truncates timestamp to the first day of the month
    -->

    <select id="getTotalProductImport" resultType="int">
        SELECT sum(qty)
        FROM tb_product_import
                 JOIN tb_product_import_detail tpid ON tb_product_import.id = tpid.product_import_id
        WHERE store_id = #{storeId}
          AND date_trunc('month', created_date) BETWEEN '${startDate}-01' AND '${endDate}-01'
    </select>

    <select id="getTotalOrderByMonth" resultType="int">
        SELECT count(*)
        FROM tb_order
                 INNER JOIN tb_order_detail tod ON tb_order.id = tod.order_id
        WHERE store_id = #{storeId}
          AND date_trunc('month', created_date) BETWEEN '${startDate}-01' AND '${endDate}-1'
    </select>

    <select id="getTotalProductSold" resultType="int">
        SELECT sum(qty)
        FROM tb_order_detail
                 INNER JOIN tb_order t ON t.id = tb_order_detail.order_id
        WHERE store_id = #{storeId}
          AND date_trunc('month', created_date) BETWEEN '${startDate}-01' AND '${endDate}-1'
    </select>

    <!-- Fixed (Step 3C): was status_id = 5, a dead id nothing ever wrote — this
         chart always returned empty/0. COMPLETED is id 6. -->
    <select id="getTotalOrderEachMonth" resultType="int">
        SELECT coalesce(count(tb_order.id), 0) AS totalMonthlyOrder
        FROM tb_order
                 JOIN tb_order_detail tod ON tb_order.id = tod.order_id
        WHERE extract(month FROM created_date) = #{startMonth}
          AND extract(year FROM created_date) = #{startYear}
          AND status_id = 6
          AND store_id = #{storeId}
        GROUP BY date_trunc('month', created_date)
    </select>

    <!-- ─── YEARLY STATS ───────────────────────────────────────────────────── -->

    <select id="getTotalOrderByYear" resultType="int">
        SELECT count(*) AS totalOrder
        FROM tb_order
                 INNER JOIN tb_order_detail tod ON tb_order.id = tod.order_id
        WHERE store_id = #{storeId}
          AND date_trunc('year', created_date) BETWEEN '${startYear}-01-01' AND '${endYear}-01-01'
    </select>

    <select id="getTotalProductImportByYear" resultType="int">
        SELECT sum(qty) AS totalProductImport
        FROM tb_product_import
                 JOIN tb_product_import_detail tpid ON tb_product_import.id = tpid.product_import_id
        WHERE store_id = #{storeId}
          AND date_trunc('year', created_date) BETWEEN '${startYear}-01-01' AND '${endYear}-01-01'
    </select>

    <select id="getTotalProductSoldByYear" resultType="int">
        SELECT sum(qty) AS totalProductSold
        FROM tb_order_detail
                 INNER JOIN tb_order t ON t.id = tb_order_detail.order_id
        WHERE store_id = #{storeId}
          AND date_trunc('year', created_date) BETWEEN '${startYear}-01-01' AND '${endYear}-01-01'
    </select>

    <!-- Fixed (Step 3C): was status_id = 5, a dead id nothing ever wrote — this
         chart always returned empty/0. COMPLETED is id 6. -->
    <select id="getTotalOrderEachYear" resultType="int">
        SELECT count(*) AS totalMonthlyOrder
        FROM tb_order
                 JOIN tb_order_detail tod ON tb_order.id = tod.order_id
        WHERE extract(year FROM created_date) = #{startYear}
          AND status_id = 6
          AND store_id = #{storeId}
        GROUP BY date_trunc('year', created_date)
    </select>

</mapper>